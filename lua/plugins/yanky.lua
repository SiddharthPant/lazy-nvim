return {
  {
    "gbprod/yanky.nvim",
    opts = {
      system_clipboard = {
        sync_with_ring = false,  -- keep yank/delete local unless a mapping targets the system clipboard
      },
    },
  },
}
